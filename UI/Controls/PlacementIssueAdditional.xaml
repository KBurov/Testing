<UserControl x:Class="Testing.UI.Controls.PlacementIssueAdditional"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <ListView Grid.Row="1" Grid.ColumnSpan="3"
                  Style="{DynamicResource PlacementImageListViewStyle}" ItemContainerStyle="{DynamicResource PlacementImageStyle}"
                  ItemsSource="{Binding Answers}" SelectedIndex="{Binding SelectedAnswerIndex}"
                  VerticalAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" />
    </Grid>
</UserControl>
